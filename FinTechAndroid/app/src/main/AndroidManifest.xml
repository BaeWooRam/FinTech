<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fintech">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".FinTechApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">
        <uses-library android:name="org.apache.http.legacy" android:required="false"/>
        <activity android:name=".ui.splashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.login.mem.Activity_Member" android:theme="@style/AppTheme.NoActionBar1"/>
        <activity android:name=".ui.login.Activity_Login" android:theme="@style/AppTheme.NoActionBar1"/>
        <activity android:name=".ui.main.Activity_Main" android:theme="@style/AppTheme.NoActionBar1"/>
        <activity android:name=".ui.goods.Activity_Goods"/>
        <activity android:name=".ui.goods.detail.Activity_GoodsDetail"/>
        <activity android:name=".ui.payment.Activity_Payment" android:theme="@style/AppTheme.NoActionBar1"/>
        <activity android:name=".ui.payment.payhistory.Activity_PayHistory"/>
    </application>

</manifest>